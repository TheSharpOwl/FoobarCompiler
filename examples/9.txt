var x : integer is 5 + 5
var y is 5.10 

routine add5(z) : integer is
    return z + 5
end


routine go5times is
    for x in reverse 5..1 loop
    end
    for x in 1..5 loop
    end

type int is integer
type float is real

type student is record
    var age : int
    var gpa : float
    var attendance : array [5 + x] bool
end

var sasha : student is record
    var age: int is 20
    var gpa: float is 90.4
    var attendance : array [15] bool
end

